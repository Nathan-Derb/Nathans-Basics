const companyName = "JsWeather";

let isSunny = "Sunny";
isSunny = "Overcast";

let isRaining = "Raining";
if (isRaining) {
  console.log(true);
} else {
  console.log(false);
}

let windSpeed = 0;
windSpeed += 14;
windSpeed -= 4;

let windDirection = "North";
windDirection = "South East";

const dayOfWeek = getDay();

let expectedPrecipitation = 0;
expectedPercipitation += 2.2;
